# DialogWidget

`DialogWidget` defines an application-internal dialog overlay. It is a normal widget and can be placed on any screen for design-time organization.

Use `OpenDialog(dialogWidgetId, origin = Screen, position = Center)` to open it. Use `CloseDialog(dialogWidgetId)` to close the matching overlay.

