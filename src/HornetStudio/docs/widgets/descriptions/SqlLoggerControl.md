SqlLoggerControl

Runtime widget for recording selected signals into a SQL-based log database.

- Stores values in a structured database file
- Supports runtime recording control from the UI
- Useful when recorded data should stay queryable

Best for:
Structured logging with later analysis needs.