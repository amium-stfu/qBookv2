# IMPLEMENTATION HANDOFF

## Goal
Ensure the repository ignores the generated release output directory `Release/`.

## Scope
Include only the `.gitignore` update needed to ignore `Release/`.
Do not modify release contents, publish settings, documentation, or unrelated ignore rules.

## Tasks
1. Open `.gitignore` at the repository root.
2. Add an ignore entry for `Release/` in the build or generated output section.
3. Verify the rule is present and does not remove existing exceptions or ignore patterns.
4. Check whether files under `Release/` are already tracked; if tracked, report that `.gitignore` will not untrack them automatically.

## Technical Constraints
- Follow the existing `.gitignore` organization and formatting.
- Keep the change minimal.
- Do not introduce new files except this handoff.
- Chat response must be in German.

## Relevant Files
- `.gitignore`
- `Release/`

## Notes
- If `Release/` is already tracked in Git, use a separate explicit decision before removing it from the index.
