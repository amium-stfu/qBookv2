# IMPLEMENTATION HANDOFF

## Goal
Replace all runtime references to `aae.png` with `Hornet.png`.

## Scope
Included:
- Update code/resource references that load `aae.png`.
- Use the existing `Hornet.png` asset.
- Verify the solution builds.

Not included:
- Do not delete `aae.png` unless explicitly requested.
- Do not rename icon files.
- Do not change unrelated icons such as `aaeBlack.png`, `aaeOrange.png`, or `aaeWhite.png`.
- Do not redesign UI or change branding text.
- Do not add new dependencies.
- Do not create a release version or Git tag.

## Tasks
1. Inspect the current working tree and avoid overwriting unrelated user changes.
2. Confirm both files exist:
   - `src/HornetStudio.Editor/EditorIcons/aae.png`
   - `src/HornetStudio.Editor/EditorIcons/Hornet.png`
3. Replace the `aae.png` resource URI in `src/HornetStudio/MainWindow.axaml.cs` with `Hornet.png`.
4. Search the repository for remaining exact `aae.png` references.
5. Do not replace similarly named assets unless they are explicitly requested.
6. Run verification:
   - `dotnet build HornetStudio.sln --no-restore`
   - If restore is required, run `dotnet build HornetStudio.sln` only if approved or already available offline.
7. Provide a final summary with changed files and build result.

## Technical Constraints
- Chat responses must be in German.
- Code, comments, XML documentation, UI text, file names, and technical identifiers must remain English.
- Keep the implementation strictly scoped to replacing `aae.png` references with `Hornet.png`.
- Use existing Avalonia resource paths and project patterns.
- Do not introduce new dependencies.

## Relevant Files
- `src/HornetStudio/MainWindow.axaml.cs`
- `src/HornetStudio.Editor/EditorIcons/aae.png`
- `src/HornetStudio.Editor/EditorIcons/Hornet.png`

## Notes
- Planning found one exact reference: `src/HornetStudio/MainWindow.axaml.cs`.
- `Hornet.png` already exists in `src/HornetStudio.Editor/EditorIcons/`.
- `aae.png` should remain in place unless the user explicitly requests asset cleanup.
