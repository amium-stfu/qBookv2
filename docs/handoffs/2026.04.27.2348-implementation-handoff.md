# IMPLEMENTATION HANDOFF

## Goal
Rename the existing `HornetStudio.Items` project to `Amium.Item` and the existing `UdlClient` project to `Amium.UdlClient` consistently across project names, namespaces, file and folder names, references, configuration, build/CI files, strings, and hard-coded references.

## Scope
Included:
- Rename `src/HornetStudio.Items/` to `src/Amium.Item/`.
- Rename `src/HornetStudio.Items/HornetStudio.Items.csproj` to `src/Amium.Item/Amium.Item.csproj`.
- Rename `src/UdlClient/` to `src/Amium.UdlClient/`.
- Rename `src/UdlClient/UdlClient.csproj` to `src/Amium.UdlClient/Amium.UdlClient.csproj`.
- Update C# namespaces, `using` statements, project references, `.sln` entries, project metadata, plugin output paths, documentation references, and hard-coded assembly/project path references.
- Run consistency checks and build verification.

Not included:
- Do not rename unrelated UI widget concepts such as `UdlClientControl` unless they are hard references to the renamed project or assembly.
- Do not change runtime behavior beyond the naming updates.
- Do not add new dependencies.
- Do not create a release version or Git tag.

## Tasks
1. Inspect the current working tree and avoid overwriting unrelated user changes.
2. Rename project folders and project files:
   - `src/HornetStudio.Items/` -> `src/Amium.Item/`
   - `src/HornetStudio.Items/HornetStudio.Items.csproj` -> `src/Amium.Item/Amium.Item.csproj`
   - `src/UdlClient/` -> `src/Amium.UdlClient/`
   - `src/UdlClient/UdlClient.csproj` -> `src/Amium.UdlClient/Amium.UdlClient.csproj`
3. Update project metadata:
   - `HornetStudio.Items` assembly/root namespace -> `Amium.Item`
   - `UdlClient` assembly/root namespace -> `Amium.UdlClient`
   - `UdlClient` plugin output path if the plugin folder should follow assembly naming, otherwise document retained runtime folder compatibility.
4. Update `HornetStudio.sln` project names and project paths while preserving project GUIDs.
5. Update all `ProjectReference` entries in dependent `.csproj` files:
   - `src/HornetStudio/HornetStudio.csproj`
   - `src/HornetStudio.Editor/HornetStudio.Editor.csproj`
   - `src/HornetStudio.Host/HornetStudio.Host.csproj`
   - `src/Amium.UdlClient/Amium.UdlClient.csproj`
6. Update namespaces and imports:
   - `namespace HornetStudio.Items` -> `namespace Amium.Item`
   - `using HornetStudio.Items;` -> `using Amium.Item;`
   - `namespace UdlClient` -> `namespace Amium.UdlClient`
   - Add or update `using Amium.UdlClient;` only where project types are referenced from outside the renamed project.
7. Update hard-coded project, assembly, and file path references in Markdown, JSON, YAML, XML, and source comments when they refer to project/assembly identity or source paths.
8. Review `src/HornetStudio.Host/plugins/UdlClient/` generated plugin artifacts:
   - Treat `.dll`, `.pdb`, and `.deps.json` files as build output.
   - Prefer cleaning/regenerating via build instead of hand-editing binaries.
   - If tracked generated files must remain, ensure generated names and `.deps.json` references match `Amium.UdlClient` and `Amium.Item`.
9. Run consistency checks:
   - Search for remaining `HornetStudio.Items`, `HornetStudio.Item`, `HornetStudio.Items.csproj`, `src\\HornetStudio.Items`, `src/HornetStudio.Items`.
   - Search for remaining project-identity `UdlClient`, `UdlClient.csproj`, `src\\UdlClient`, `src/UdlClient`.
   - Separately review remaining `UdlClientControl`, `HostUdlClient`, runtime paths such as `Runtime.UdlClient`, and UI labels to decide whether they are intentionally retained product/domain names.
10. Run build verification:
    - `dotnet build HornetStudio.sln --no-restore`
    - If restore is required because project paths changed, run `dotnet build HornetStudio.sln` with approval if network access is needed.
11. Update `CHANGELOG.md` with a concise unreleased entry if the repository uses it for local relevant changes.
12. Provide a final summary listing every changed file and note any intentionally retained `UdlClient` domain/UI/runtime names.

## Technical Constraints
- Chat responses must be in German.
- Code, comments, XML documentation, UI text, file names, and technical identifiers must remain English.
- Keep the change strictly scoped to the requested rename.
- Follow existing `.editorconfig` and project formatting.
- Do not introduce new dependencies.
- Prefer existing project structure under `src/`.
- Public API namespace changes are breaking changes and must be called out in the final summary.
- Generated build artifacts should be regenerated rather than manually edited when possible.

## Relevant Files (if known)
- `HornetStudio.sln`
- `src/HornetStudio.Items/HornetStudio.Items.csproj`
- `src/UdlClient/UdlClient.csproj`
- `src/HornetStudio/HornetStudio.csproj`
- `src/HornetStudio.Editor/HornetStudio.Editor.csproj`
- `src/HornetStudio.Host/HornetStudio.Host.csproj`
- `src/HornetStudio.Items/**/*.cs`
- `src/UdlClient/**/*.cs`
- `src/HornetStudio.Host/**/*.cs`
- `src/HornetStudio.Editor/**/*.cs`
- `src/HornetStudio/**/*.cs`
- `src/HornetStudio.Host/plugins/README.md`
- `src/HornetStudio.Host/plugins/UdlClient/UdlClient.deps.json`
- `docs/**/*.md`
- `src/HornetStudio/docs/**/*.md`
- `src/HornetStudio.Editor/**/*.yaml`
- `src/HornetStudio/Folder.yaml`
- `src/HornetStudio/Folder.json`
- `CHANGELOG.md`

## Notes
- The user requested `HornetStudio.Item`, but the repository currently contains `HornetStudio.Items`. Treat the existing `HornetStudio.Items` project as the source project and rename it to the requested singular `Amium.Item`.
- Be careful not to blindly replace every `UdlClient` occurrence. Many references are widget names, persisted layout keys, runtime path segments, or domain labels. Only rename those when they represent project, assembly, namespace, source folder, or hard-coded project references.
- `Amium.Item` is singular by request; do not change it to `Amium.Items` unless the user explicitly corrects the target name.
