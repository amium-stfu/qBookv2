UdlClientControl

Connection widget for UDL communication and attached runtime items.

- Manages host, port, and connection state
- Exposes attachable runtime data inside the UI
- Useful for integrations with external UDL sources

Best for:
Projects that need direct UDL connectivity in the editor UI.